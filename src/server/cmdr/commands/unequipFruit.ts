import { CommandDefinition } from "@rbxts/cmdr";

export = identity<CommandDefinition>({
	Name: "unequipfruit",
	Aliases: ["unequip"],
	Description: "Desequipa la fruta del diablo actual",
	Group: "Admin",
	Args: [],
}); 
